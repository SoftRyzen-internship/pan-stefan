type LinkButtonProps = {
  link: { target: string };
  text: string;
  centered?: boolean;
  long?: boolean;
  lightHover?: boolean;
  xwide?: boolean;
  download?: boolean;
};

export default LinkButtonProps;
